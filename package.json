{
  "name": "karin-keren-financial",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm --prefix compare-app ci --no-audit --no-fund && npm --prefix compare-app run build",
    "build:site": "npm --prefix compare-app run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "twilio": "^5.13.1",
    "@anthropic-ai/sdk": "^0.71.0",
    "pg": "^8.21.0"
  },
  "engines": {
    "node": ">=18"
  },
  "description": "Karin Keren – תכנון פיננסי: fund-comparison website (React) + client portal + Mia AI agent + WhatsApp webhook"
}
