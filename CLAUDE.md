# CLAUDE.md — הנחיות והעדפות לפרויקט

## העדפה מרכזית של קרין (בעלת העסק)
- **בונים כאן, לא ב-Base44.** קרין החליטה שלא לחדש את המנוי ל-Base44 (עלה מעל 12,000 ₪ בשנה — יקר מדי).
  כל פיתוח, אוטומציה, טופס, מסך לקוח או פיצ'ר חדש — נבנה בתוך `karin-webhook` ומתארח על Railway בעלות נמוכה.
- לפני שמציעים כלי/שירות חיצוני בתשלום, קודם לבדוק אם אפשר לבנות את זה כאן בעלות אפסית.

## על הפרויקט
`karin-webhook` הוא כל המערכת הדיגיטלית של קרין קרן – סוכנות לביטוח ופיננסים:
- **אתר ציבורי** — `public/` (Express static).
- **פורטל לקוחות** — התחברות ב-OTP (SMS דרך Twilio) מול נתוני לקוח מ-**ראוטרו/Roeto API**.
- **Mia** — סוכן שירות לקוחות (צ'אט webhook + WhatsApp Cloud API). מנוע התשובות ב-`agent.js`.
- **טופס יצירת קשר** — פותח קישור וואטסאפ (לא שולח מייל).

קבצים מרכזיים: `index.js` (שרת + API), `agent.js` (Mia), `whatsapp.js` (ערוץ וואטסאפ),
`store.js` (היסטוריית שיחות — זיכרון/Postgres), `.env.example` (כל משתני הסביבה).

## החלטות/מצב נוכחי
- **תשובות אוטומטיות של Mia מבוטלות כברירת מחדל.** מתג `MIA_AUTO_REPLY` (ב-`agent.js`) —
  כבוי כברירת מחדל; הודעות נכנסות מתקבלות אך אין מענה אוטומטי. להפעלה: `MIA_AUTO_REPLY=true`.

## סגנון עבודה
- לענות לקרין בעברית.
- לא לשלוח מיילים אוטומטיים ללקוחות ללא אישור מפורש.
